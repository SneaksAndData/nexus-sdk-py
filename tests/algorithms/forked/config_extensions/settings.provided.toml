dynaconf_merge = true

[runtime.payload]

# payload types supported by this Nexus application. At least one must be provided
types = [
    "tests.algorithms.forked.sample_main_forked.TestForkedAlgorithmPayload"
]


# ADDITIONAL SERVICES
[services]
    # Query Enabled Stores
    [query_enabled_store]

    enabled = "0"
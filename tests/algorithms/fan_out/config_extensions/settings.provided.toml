dynaconf_merge = true

[runtime.payload]

# payload types supported by this Nexus application. At least one must be provided
types = [
    "tests.algorithms.fan_out.sample_main_fan_out.TestFanOutAlgorithmPayload"
]


# ADDITIONAL SERVICES
[services]
    # Query Enabled Stores
    [query_enabled_store]

    enabled = "0"
